// JavaScript File
var userAmount = prompt ("What is the amount of the item?", "");
var amount = Number(userAmount);
var tax = amount * 0.095;
var totalPrice = amount + tax;
alert("the item price is $"+ amount + "the Tax amount is $" + tax + " , and the total Price is $" + totalPrice + ".")

